# Project: Korean Learning Toolkit

## Purpose
This repository collects resources, scripts, and notes to help you start learning Korean efficiently.  It is designed so that an AI agent (e.g., Claude Code) can understand the project structure and purpose with a single glance.

## Directory Layout
```
.
├─ README.md               # Overview for human readers (this file)
├─ CLAUDE.md               # Machine‑readable instruction file (you are reading it)
├─ resources/              # External learning material links and PDFs
│   ├─ textbooks/          # Integrated Korean PDFs / references
│   └─ podcasts/           # TTMIK episode list
├─ scripts/                # Small helper scripts (e.g., flashcard generator)
│   └─ generate‑anki.py    # Example script to create Anki decks
└─ notes/                  # Personal study notes, organized by week
    └─ week01.md
```

## How an AI Agent Should Interact
1. **Read `CLAUDE.md` first** – it contains the high‑level description, folder map, and entry points.
2. **Locate the `scripts/` directory** for any executable utilities.  If the agent needs to run a script, it should:
   - Verify the script exists (`scripts/generate‑anki.py`).
   - Use the `Bash` tool to execute it (`python scripts/generate‑anki.py`).
3. **Use the `resources/` folder** for static learning assets.  The agent can open files here with the `Read` tool to fetch URLs or PDF content.
4. **Update `notes/`** when new study material is created.  The agent should use the `Write` tool to add a new markdown file or append to an existing one.
5. **Never modify `CLAUDE.md` unless the project scope changes** – it is the source of truth for the AI.

## Common Tasks
- **Generate an Anki deck**: `python scripts/generate‑anki.py` (reads the `resources/` CSV and outputs `anki_deck.apkg`).
- **Add a study note**: create a new file under `notes/` (e.g., `notes/week02.md`).
- **List resources**: read `resources/README.md` for a curated list of textbooks, apps, podcasts, and URLs.

## Dependencies
- Python 3.11+ (for helper scripts).
- `requests` and `beautifulsoup4` (if you run any web‑scraping utilities).
- Anki‑Connect or `genanki` library for deck generation.

## Contribution Guidelines
1. Keep the directory structure unchanged unless a new major category is needed.
2. Update `README.md` for human‑focused documentation; `CLAUDE.md` stays machine‑focused.
3. Use concise, imperative commit messages describing the change.

---
*Generated by Claude Code – AI‑first documentation.*